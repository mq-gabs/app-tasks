import { StyledText, textSize } from "../../../layouts";
import styled from "styled-components";


export const StyledTitleText = styled(StyledText)`
    font-size: ${textSize.md};
`

export const StyledDescriptionText = styled(StyledText)`
    font-size: ${textSize.md};
`

export const StyledForm = styled.form`
    width: fit-content;
    margin: auto;
    text-align: justify;
    
`